#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	float a,b,c;
	a=7.2;
	b=4;
	cout<<ceil(a)<<endl;
	//rounds digit to the closest higher number 
	cout<<floor(a)<<endl;
	//rounds digit to closest lowest number
	cout<<pow(a,b)<<endl;
	//used for power(write number first,then write its power)
	cout<<log(a)<<endl;
	//used for log with base 2
	cout<<log10(a)<<endl;
	//used for log with base 10
	cout<<sqrt(a)<<endl;
	//sqrt used for square root
}
